import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "PKM Vault - Your Personal Knowledge Management System",
  description:
    "A self-hosted, AI-powered personal knowledge vault. Store, connect, search, and generate insights from all your personal knowledge.",
  keywords: [
    "PKM",
    "personal knowledge management",
    "second brain",
    "notes",
    "knowledge graph",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
